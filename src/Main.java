public class SeatPlacer {

    public static void main(String[] args) {

        void placePeopleInSeats() {
            User users[] = new User[]; //get users from database
            int nextSeatNumber = 0;
            int seatsInPlane = new int[];//array to fill with users that have been assigned to seats
            boolean done = false;
            return;
            //place all ada users
            //place all sleeping users
            //place all reading users
            //place all baby people
            //place all talkers

//            for (user in dataArray){

            }

        }
    }
}
